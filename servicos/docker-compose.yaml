version: '3'

services:
# Serviço Web do Nginx para os arquivos estáticos
  webserver-static:
    container_name: webserver-static
    restart: always
    build:
      context: ./static
      dockerfile: Dockerfile
    ports:
      - '8009:80'
    volumes:
      - './static/html:/usr/share/nginx/html'

# Serviço de banco de dados com PostgreSQL
  database:
    container_name: database
    image: postgres
    hostname: database
    restart: always
    environment:
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}      
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

  # Serviço do Laravel 10
  # O host 'database' se refere ao nome do servico do banco de dados, descrito acima
  api:
    container_name: api
    build:
      args:
          user: user
          uid: 1000
      context: ./api
      dockerfile: Dockerfile
    restart: always
    environment:
      DB_HOST: ${DATABASE_HOST}
      DB_PORT: ${DATABASE_PORT}
      DB_DATABASE: ${DATABASE_NAME}
      DB_USERNAME: ${DATABASE_USERNAME}
      DB_PASSWORD: ${DATABASE_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      APP_KEY: ${APP_KEY}
      READ_ONLY_API_TOKEN: f4c9d20d8bb64156a9ce2e1b2ff0c4a7a0d1fa6b7e3c4f8a9b0c1d2e3f4a5b6c
    ports:
      - '9000:80'
    volumes:
      - ./api/src:/var/www/html
      - ./static/html/objetos:/var/www/html/storage/objetos
    working_dir: /var/www/html
    depends_on:
      - database
      
volumes:
  postgres_data: